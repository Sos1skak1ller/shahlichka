# TO-DO — Персональный игровой слой Х5 Клуб

Составлено 2026-09-04 по итогам аудита репозитория, `docs/TZ_X5_gaming_layer.md`
(§10 «ML System Design», §13 «Риски и открытые вопросы», §15) и `quickstart-results.md`
(блок «Открытые пункты»).

---

## 0. Где мы сейчас (базовая линия, проверено прогоном)

- Все 81 задача из `specs/001-gaming-layer/tasks.md` отмечены `[X]`.
- Тесты зелёные: **engine 65 / sim 19 / web 15**; `ruff check` — чисто; `scripts/check-parity.sh` — `parity OK`; `npm run build` собирается.
- Симуляция `run-10000-4w-s42`: ROI **+384 255 ₽**, инвариант «награда ≤ прирост маржи» выполняется, antifraud precision **0.977**.
- Реально открыто: одна поломанная CLI-ветка (см. 1.0), ручная разметка релевантности (SC-001 / SC-002), незакрытые вопросы ТЗ §13, пустой раздел «Команда и роли», нет финального имени продукта, цифры маржи — иллюстративные.

**Вывод:** код-контур MVP закрыт на 99%. Оставшееся — один баг и то, без чего питч разваливается на вопросах жюри.

---

## 1. НАДО ДОДЕЛАТЬ СЕЙЧАС (блокеры к защите)

### 1.0 ❗ Починить выгрузку выборки для SC-002 — единственный оставшийся код-блокер

`specs/001-gaming-layer/eval/promo-relevance.md` документирует команду
`python -m gaming_sim.eval_dump --kind promo …`, но в `sim/src/gaming_sim/eval_dump.py:55`
стоит `choices=["challenge"]`, и команда падает:
`error: argument --kind: invalid choice: 'promo'`.
Выборку для разметки акций сейчас получить нечем — **SC-002 физически не закрыть**.

- [ ] Добавить `promo` в `--kind` и ветку дампа в формате из `promo-relevance.md`: `{profile_id, segment, top_categories, ranked_promos:[{promo_id, category, rank_score, margin_impact}]}` (топ-3).
- [ ] Проверить: `uv run python -m gaming_sim.eval_dump --kind promo --n 40 --seed 11 --out specs/001-gaming-layer/eval/promo-samples.jsonl`.
- [ ] Тест на оба режима `--kind`, чтобы документация и CLI больше не расходились.

### 1.1 ❗ Экспертная разметка релевантности — SC-001 (челленджи) и SC-002 (акции)

В части акций зависит от 1.0.

- [ ] Сгенерировать выборки:
  `uv run python -m gaming_sim.eval_dump --kind challenge --n 40 --seed 7 --out specs/001-gaming-layer/eval/challenge-samples.jsonl`
  и то же `--kind promo --seed 11` (после 1.0).
- [ ] Назначить размечающего (открытый вопрос ТЗ §13 №3) и пройти по чек-листам `eval/challenge-relevance.md` и `eval/promo-relevance.md`.
- [ ] Занести таблицу результатов и итог (`# релевантных / # размеченных`) в эти же файлы + строку в `quickstart-results.md`.
- [ ] Если < 70% — вернуться к T081: перетюнить веса `engine/src/gaming_engine/challenge/ranker.py` / эвристику `promo.py`, заморозить сериализованные артефакты.
- **Критерий готовности:** оба показателя ≥ 0.70, числа лежат в репозитории, воспроизводимы по seed.

### 1.2 ❗ Зафиксировать иллюстративные цифры экономики — ТЗ §12, §13 №2

- [ ] Явно выписать принятые допущения: маржа ₽/чек (сейчас 45 ₽), средний чек, недельная маржа по архетипам (`config.WEEKLY_MARGIN_BY_ARCHETYPE`).
- [ ] Держать одно место истины — `engine/src/gaming_engine/config.py`; каждая константа с пометкой «illustrative — team to ratify»; сверить с `pilot-plan.md`.
- [ ] Дисклеймер «цифры маржи — иллюстративные, кейс их не даёт» на слайд пилота и в README.
- **Критерий:** ревьюер видит, откуда взялся ROI, и что это допущение, а не факт о X5.

### 1.3 Закрыть открытые вопросы ТЗ §13

- [ ] №1 — ратификация сегмента «родители с детьми до 3 лет» командой; статус в `spec.md` / ТЗ → ✅.
- [ ] №4 — выбрать значение `ANTI_FATIGUE_N`, обосновать одной строкой.
- [ ] №6 — зафиксировать финальный набор антифрод-признаков и порог как «ратифицировано», снять ⚠️.
- [ ] №7 — финальное имя продукта без «ИИ»/«AI» (сейчас плейсхолдер «Х5 Клуб · Рост»).
- [ ] №5 — глубина имитации для каждого пункта «вне скоупа»: одна строка на пункт.

### 1.4 ❗ Заполнить раздел «Команда и роли» — ТЗ §15 (сейчас пуст)

- [ ] Кто отвечает за: продукт / ML+антифрод / фронт демо / симуляцию.
- [ ] Отдельный абзац «как использовали AI в процессе подготовки» — это отдельный пункт кейса.

### 1.5 Прогон демо начисто

- [ ] `cd web && npm run dev` — пройти 3 экрана + режим сравнения (left/right), проверить мобильный layout.
- [ ] Прогнать все 6 сценариев `quickstart.md` на чистом клоне, обновить дату в `quickstart-results.md`.
- [ ] Оффлайн-проверка: dev-сервер без сети, ноль сетевых запросов (DevTools → Network).

---

## 2. СКОП ПО РЕКОМЕНДАЦИЯМ (улучшения, не блокеры)

### 2.1 Демо-фронт / визуал

- [ ] Подключить референс-арт маскота (`image_reference/*.png`): свежий апельсин/листик на прогрессе, «сухой» вариант при сбросе стрика — сейчас маскот рисуется SVG (`web/src/components/Mascot.tsx`), PNG-референсы не задействованы.
- [ ] Экран челленджа: показать «почему именно этот челлендж» (топ-фичи ранжирования) — усиливает тезис «объяснимость важнее сложности».
- [ ] Экран профиля: визуализировать недельный бюджетный потолок и инвариант «награда ≤ прирост маржи».
- [ ] Экран реферала: показать машину состояний (`invited → registered → purchase_confirmed → reward_released`) и `block_reason` на примере self-referral.
- [ ] Явно показать на демо cold-start (`generated_by = fallback`) и пустые состояния, а не прятать их.
- [ ] Вывести на экран заметки движка: `template_pool_exhausted`, anti-fatigue-переключение.
- [ ] A11y-проход: фокус, `aria` на табах, контраст.

### 2.2 ML / персонализация — апгрейды из ТЗ §10 («если останется время»)

- [ ] Ранжирование челленджей: логрег → LightGBM на синтетических метках отклика из Simulation Engine (§10.1).
- [ ] Таймер механик: rule-based → contextual bandit (LinUCB / Thompson sampling) (§10.2) — опционально.
- [ ] Ранжирование акций: эвристика → LightGBM с таргетом «отклик в симуляции» (§10.3).
- [ ] Антифрод: scorecard → градиентный бустинг + SHAP по фиче; порог решения остаётся фиксированным и объяснимым (§10.4).
- [ ] Проставлять `algo_version` в `RecommendationLog` и в отчёте симуляции — трассируемость.
- **Правило:** любой апгрейд модели не ломает детерминизм demo↔sim (SC-006) — сериализованные веса, фиксированный seed.

### 2.3 Антифрод / экономика

- [ ] Показывать recall и матрицу ошибок рядом с precision (recall не гейтит, но нужен для честности).
- [ ] Тест на границе порога для каждой полосы (`block` / `review` / `pass`) — сейчас только block-boundary.
- [ ] Сценарий «новый риск-сигнал во время review-hold → block» вынести в демо-фикстуру, не только юнит-тест.
- [ ] Sensitivity-анализ ROI к ±20% по марже — абзац в `pilot-plan.md`.

### 2.4 Симуляция

- [ ] Прогоны на нескольких seed (напр. 5) → доверительный интервал по primary-метрикам вместо одного числа.
- [ ] Пятый архетип (ТЗ §10.5 «опционально пятый») — если добавит реализма.
- [ ] Зафиксировать оба края диапазона: прогоны 1k и 10k в `quickstart-results.md`.
- [ ] Тест «treatment деградирует guardrail → отчёт это помечает».

### 2.5 Инфра / инженерка

- [ ] CI: добавить web-lint (ESLint / Prettier) — конфиг есть, в `.github/workflows/ci.yml` не запускается.
- [ ] CI: guard на `scripts/gen-contract-types.sh` — сгенерированные типы не разошлись со схемами.
- [ ] CI: кэш uv / npm; бейдж статуса в README.
- [ ] Убрать из репозитория артефакт сборки `web/tsconfig.tsbuildinfo` (сейчас закоммичен и шумит в диффах — попал в PoC_v3). `sim/out/` уже в `.gitignore`, `fixtures/out/*.json` коммитятся осознанно — эти два трогать не нужно.

### 2.6 Документация / защита

- [ ] Слайд «осознанные отклонения» (LLM → ML, рейтинг экономии убран) — тезисы в README есть, оформить визуально.
- [ ] Диаграмма core loop и сервисов (ТЗ §8.2) — картинкой.
- [ ] FAQ жюри: «почему не LLM», «как обеспечена анонимность без рейтинга экономии», «откуда цифры маржи», «precision vs recall».

---

## 3. Вне скоупа (осознанно не делаем) — ТЗ §14

Интеграция с боевыми CRM / кассами / push, промышленная нагрузка, реальные списания,
ПДн и внутренние витрины X5, полный антифрод-контур, модерация контента, магазин
скинов, отдельное приложение, полный юридический / секьюрити-анализ.
